import { JSXElement } from "solid-js"

interface NavProps {
    icon: JSXElement
    text: string
    onClick?: () => void
}

export const Navbar = () => {
    return (
        <div>
            
        </div>
    )
}